# Public C ABI of libmirage-sdcpp.a. Anything not listed here is hidden
# (localised to the archive) during partial linking so it cannot collide
# with another copy of ggml/llama.cpp linked into the same binary —
# specifically Kuzco's vendored llama.cpp, which ships its own
# `_ggml_metal_library_init` etc. Without this, Apple's ld picks the
# first definition it finds and sd.cpp ends up calling into Kuzco's
# (older, stale-metallib-pinned) ggml-metal at runtime.
#
# Keep in sync with MirageC.h.
_mirage_ctx_create
_mirage_ctx_free
_mirage_generate
_mirage_free_image
_mirage_last_error
_mirage_version
_mirage_releases_component_weights_after_use
_mirage_chroma_uses_safe_dit_mask_configuration
_mirage_set_progress_callback
